<resources>
    <!--
      Only styles the window before Compose draws its first frame.
      The real Material 3 theme lives in ui/theme/Theme.kt.
    -->
    <style name="Theme.RecipeApp" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
